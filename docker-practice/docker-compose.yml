services:
  web:
    image: golang
    tty: true
    ports:
      - 8080:8080
    environment:
      DB_HOST: db
    depends_on:
      - db
  db:
    image: mysql:8.0
    environment:
      MYSQL_ROOT_PASSWORD: root